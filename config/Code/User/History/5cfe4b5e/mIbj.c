#include <klibtest.h>
#include <limits.h>

void sprintf_test() {
    int data[] = {0,           INT_MAX / 17,  INT_MAX,      INT_MIN,
                  INT_MIN + 1, UINT_MAX / 17, INT_MAX / 17, UINT_MAX};
}

void read_test() {
    sprintf_test();
}