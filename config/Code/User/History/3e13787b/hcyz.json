{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Gdb Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/build/${fileBasenameNoExtension}.out",
            "cwd": "${fileDirname}",
            "preLaunchTask": "Compile"
        }
    ]
}